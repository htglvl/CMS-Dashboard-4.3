{
  "name": "@cms-dashboard/openclaw-plugin",
  "version": "1.0.0",
  "type": "module",
  "description": "OpenClaw plugin for CMS Grid Resilience Dashboard — risk queries, outage analysis, charging site lookups, and incident notifications.",
  "scripts": {
    "build": "tsc",
    "build:plugin": "npm run build && openclaw plugins build --entry ./dist/index.js"
  },
  "openclaw": {
    "extensions": [
      "./index.ts",
      "./dist/index.js"
    ],
    "compat": {
      "pluginApi": ">=2026.3.24-beta.2",
      "minGatewayVersion": "2026.3.24-beta.2"
    }
  },
  "dependencies": {
    "typebox": "^1.3.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
